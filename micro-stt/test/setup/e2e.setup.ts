// E2E setup: allow network for localhost; avoid global nock network blocking
// If specific external calls must be mocked, do so per-test rather than globally.
// Timeout is configured in jest-e2e.json via testTimeout
