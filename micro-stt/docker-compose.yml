services:
  micro-stt:
    build: .
    container_name: micro-stt
    ports:
      - '3001:3001'
    environment:
      - NODE_ENV=production
      - LISTEN_HOST=0.0.0.0
      - LISTEN_PORT=3001
    restart: unless-stopped
